{
  "name": "agents-office",
  "version": "3.6.1-beta.1",
  "description": "Agents Office v3 (Beta) — a 3D isometric office where AI agents do real work on your own Claude login",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "tsx serve.ts",
    "dev": "vite",
    "build": "tsx build.ts",
    "check": "tsx check.ts",
    "check:live": "CHECK_LIVE=1 tsx check.ts",
    "graph": "tsx graph-build.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "d3-force": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.0",
    "esbuild": "^0.28.1",
    "playwright-core": "^1.62.0",
    "three": "^0.185.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  },
  "optionalDependencies": {
    "@anthropic-ai/sdk": "^0.80.0"
  }
}
